Reproduction steps:

- `pnpm install`
- `npx vue-tsc --noEmit`

You will see exactly two errors, both `TS2615`, one for `parent` and one for `children`.

Install @regle packages version 1.17.0, no issues.
